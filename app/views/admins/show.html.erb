<%= render "layouts/header" %>
<div class="content">

<h1> Users in Database </h1>
<p></p>
<table>
  <tr>
    <th>ID</th>
    <th>Email Address</th>
    <th colspan="3"></th>
  </tr>

<% @users.each do |users| %>
   <tr>
     <td><%= users.id %></td>
     <td><%= users.email %></td>
   </tr>
  
   <% end %>
</table>

</div>
<%= render "layouts/footer" %>